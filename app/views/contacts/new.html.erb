<h2><i class="fas fa-envelope mr-3"></i>お問い合わせ</h2>
<hr />
<p>下記フォームよりお問い合わせください。</p>

<div class="d-flex justify-content-center">
  <div class="col-7 mt-5">
    <%= form_with(model: @contact, local: true) do |f| %>

    <div class="form-group">
      <%= f.label :title %>
      <%# プルダウンの選択肢を作成している、引数は保存先のカラム、 [表示、実際にDBに保存させる内容]%>
      <%= f.select :title, [["サービスについて", "サービスについて"], ["採用について", "採用について"], ["請求について", "請求について"]], include_blank: "選択して下さい", class: "form-control" %>
    </div>

    <div class="form-group">
      <%= f.label :name %>
      <%= f.text_field :name, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= f.label :email %>
      <%= f.text_field :email, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= f.label :content %>
      <%= f.text_area :content, class: "form-control", rows: 7 %>
    </div>

    <%= f.submit "送信する", class: "btn btn-success text-white btn-lg btn-block mt-5" %>
    <% end %>
  </div>
</div>